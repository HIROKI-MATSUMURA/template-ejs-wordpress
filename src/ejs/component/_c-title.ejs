<!-- 有効な色のリストを定義 -->
<% const positions = ['right']; %>
<% const positionClass = positions.includes(position) ? ` c-title__sub--${position}` : ''; %>
<div class="c-title">
  <h2 class="c-title__main"><%= ja %></h2>
  <p class="c-title__sub<%- positionClass %>"><%= en %></p>
</div>

